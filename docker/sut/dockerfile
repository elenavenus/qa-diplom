# Используем официальный образ с JDK в качестве базового
FROM openjdk:11

# Определяем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл сборки Spring Boot приложения (example.jar) в контейнер
COPY . .

# Указываем порт, на котором работает ваше приложение
EXPOSE 8080

# Команда для запуска Spring Boot приложения
ENTRYPOINT ["java", "-jar", "aqa-shop.jar"]